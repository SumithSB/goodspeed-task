# Web (`apps/web`)

Next.js frontend — documents, chat (streaming + session sidebar), upload, usage dashboard.

**Setup, env vars, migrations, and verification:** see the [root README](../../README.md).

```bash
# from repo root (recommended)
pnpm setup && pnpm dev

# web only (from this directory)
pnpm dev
```

Open http://localhost:3000
